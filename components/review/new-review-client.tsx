"use client";
import { useState } from "react";import { useSearchParams } from "next/navigation";import { RestaurantSelector } from "./restaurant-selector";import { ReviewForm } from "./review-form";import { useAppContext } from "@/hooks/use-app-context";import type { Restaurant } from "@/types";
export function NewReviewClient(){const {restaurants}=useAppContext();const params=useSearchParams();const preset=restaurants.find(r=>r.slug===params.get("restaurant"));const [selected,setSelected]=useState<Restaurant|null>(preset??null);return selected?<ReviewForm restaurant={selected}/>:<RestaurantSelector onSelect={setSelected}/>;}
